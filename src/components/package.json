{
  "name": "@torr-ui/components",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsup src/**/*.ts --dts --format esm,cjs --minify --clean",
    "changeset": "npx changeset"
  },
  "peerDependencies": {
    "react": ">=17.0.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}
